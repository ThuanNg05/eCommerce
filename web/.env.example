# Vercel / Production deployment:
# Set VITE_API_BASE to your backend URL (e.g. Render backend service)
# Example: VITE_API_BASE=https://<render-service>.onrender.com
#
# Note: For local development with Vite dev server proxy, leave empty.
VITE_API_BASE=
